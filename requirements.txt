# >=2.7.1 required since Discord's DAVE/E2EE voice enforcement (Mar 2026):
# older versions get kicked from voice with 4017 in a connect/disconnect loop.
# NOTE: voice RECEIVE is still broken even on 2.7.1 — frames arrive E2EE and
# discord-ext-voice-recv can't decrypt DAVE yet (voice-recv issue #53).
discord.py[voice]>=2.7.1,<3
discord-ext-voice-recv==0.5.2a179
edge-tts>=6.1
fastapi>=0.115
uvicorn>=0.30
aiosqlite>=0.20
httpx>=0.27
ddgs>=9
