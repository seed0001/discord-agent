# Voice capture happens in the Node.js sidecar (listener/) — Discord's
# DAVE/E2EE voice enforcement (Mar 2026) has no Python receive support yet,
# so the Python side never connects to voice and needs no voice extras.
discord.py>=2.7.1,<3
edge-tts>=6.1
fastapi>=0.115
uvicorn>=0.30
aiosqlite>=0.20
httpx>=0.27
ddgs>=9
# Build / development
maturin>=1.5.0
pypdf>=4
python-docx>=1.1
e2b>=2,<3

