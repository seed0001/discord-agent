# Extra system packages for voice support:
# libopus — decoding incoming voice streams (discord-ext-voice-recv)
# ffmpeg  — playing TTS audio back into the channel
[phases.setup]
nixPkgs = ["...", "ffmpeg", "libopus"]
